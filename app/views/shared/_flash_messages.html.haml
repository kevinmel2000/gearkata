- if flash.any?
  .row
    .twelve.columns
      .flash
        - flash.each do |name, msg|
          %div.flash-event{ class: name }
            .row
              .eleven.columns
                = msg
              .one.column.text-r
                %span.text-c.close-x ✖︎
